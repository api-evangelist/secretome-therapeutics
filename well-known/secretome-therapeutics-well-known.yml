generated: '2026-08-26'
method: probed
source: >-
  Direct HTTPS probes of every /.well-known/ path in the pipeline contract against the only
  host Secretome Therapeutics operates (secretometherapeutics.com). secretometx.com — the
  domain behind the company's info@secretometx.com address — 301s to the same origin and was
  probed through that redirect.
note: >-
  NO DOCUMENT WAS SERVED. The corporate site runs on the VendorGroup/Kscope investor-relations
  platform, which answers HTTP 200 with the rendered homepage for EVERY unknown path — /llms.txt,
  /robots.txt, /sitemap.xml and all seven /.well-known/ paths returned 200 with
  Content-Type text/html and a body beginning "<!DOCTYPE html>" and titled "Secretome | Home".
  These are soft-404s, not documents. Per the pipeline contract a 200 returning an HTML/SPA shell
  is a MISS, so no WellKnown, APICatalog or SecurityTxt pointer is emitted from this file — the
  provider serves none of these surfaces.
hosts:
- host: secretometherapeutics.com
  documents:
  - path: /.well-known/security.txt
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: soft-404 — platform catch-all returned the homepage HTML, not an RFC 9116 document
    file: null
  - path: /.well-known/openid-configuration
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: soft-404 — homepage HTML, not JSON discovery metadata
    file: null
  - path: /.well-known/oauth-authorization-server
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: soft-404 — homepage HTML, not RFC 8414 metadata
    file: null
  - path: /.well-known/api-catalog
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: soft-404 — homepage HTML, not an RFC 9727 linkset
    file: null
  - path: /.well-known/ai-plugin.json
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: soft-404 — homepage HTML, not a plugin manifest
    file: null
  - path: /.well-known/agent-card.json
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: >-
      soft-404 — homepage HTML, not an A2A AgentCard. This is exactly the SPA/catch-all false
      positive the contract warns about; no a2a/ artifact was written.
    file: null
  - path: /.well-known/agent.json
    status: 200
    served: false
    content_type: text/html; charset=utf-8
    note: soft-404 — homepage HTML, not a pre-0.3 AgentCard
    file: null
